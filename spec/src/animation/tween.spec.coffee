define ['animation/tween'], (Tween) ->
	describe 'Animation - Tween', ->
		describe '#constructor()', ->
			it 'should fall back to sensible defaults when parameters are missing', ->
				

			it 'should accept numbers and vectors', ->
				

			it 'should also accept other things, once it knows about them', ->
				
